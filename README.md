# soc-log-analyzer
A Python-based Security Operations Center (SOC) log analysis tool that simulates  real-world SIEM (Security Information and Event Management) functionality.  This tool automatically reads server log files, detects security threats using  pattern matching, flags suspicious IP addresses, and generates a structured  incident report.
